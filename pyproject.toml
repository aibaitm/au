[project]
name = "au"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hk", email = "1076767262@qq.com" }
]
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.60.0",
]

[project.scripts]
au = "au:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
